import 'package:flutter/material.dart';

class ChallengeResponse {
  String subject;
  String description;
  String imgIcon;
  String due;
  Color bgColor;

  ChallengeResponse(
      this.subject, this.description, this.imgIcon, this.due, this.bgColor);
}
